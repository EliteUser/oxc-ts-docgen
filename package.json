{
  "name": "oxc-ts-docgen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint .",
    "fmt": "oxfmt .",
    "dev": "pnpm --filter playground run dev"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "tsup": "^8.5.0",
    "typescript": "~6.0.2",
    "vitest": "^3.2.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "oxc-parser",
      "oxc-resolver"
    ]
  }
}
